"use client";

/**
 * HeroCTA — deprecated. CTA is now embedded directly in HeroSection
 * as a shared ViewStoriesCTA component for both mobile and desktop.
 * This file is kept for component structure parity but renders nothing.
 */
export default function HeroCTA({ variant }: { variant: "mobile" | "desktop" }) {
  return null;
}
